{
  "name": "subagent-router",
  "version": "0.1.0",
  "private": true,
  "description": "OMD 理念在 DeepSeek Harness 上的插件式迁移：模式能力边界 + 按模式配模型 + 同模式差异化子代理调用",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @subagent-router/omd-dsh",
    "test": "npm run test --workspace @subagent-router/omd-dsh",
    "pack": "npm pack --workspace @subagent-router/omd-dsh"
  },
  "license": "MIT"
}
